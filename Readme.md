The following code is a lesson on method overload, meaning that methods can have the same name as eachother but they can perform different actions such as one that holds int, string and doubles, etc.
